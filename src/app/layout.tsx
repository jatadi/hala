import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Hala - Track Every Race",
  description: "The social network for sports fans. Log every F1 race you watch, rate your favorites, and discover what your friends are watching.",
  keywords: ["sports", "F1", "Formula 1", "social", "tracking", "diary"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
